<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class VerActividad extends Model
{
    /*$estado = Estado::find(1);
$municipios = $estado->municipios(); // Se supone que un estado tiene N municipios
foreach($municipios as $municipio){
    $eventos = $municipio->evento(); // evento() es la relación que deberías tener en tu modelo de Municipio. Esto devuelve una colección de N eventos.
}*/
    
}
